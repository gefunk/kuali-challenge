
let Elevator = require ("./elevator");

/**
* Represents the controller that keeps track of the positions of elevators
* requests elevators to floors
*/
class Controller{
    constructor(num_floors, num_elevators){
    
    }
    
}